<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- btn_general text color when not activated -->
    <item android:state_activated="false"
        android:color="@color/non_active_button_text_color" />

    <!-- btn_general text color when active -->
    <item android:state_activated="true"
        android:color="@color/active_button_text_color" />

</selector>